# 快速开始（5 分钟上手）

## 一、准备工作（2 分钟）

### 选项 A：直接使用（推荐）
1. 下载预编译 APK
2. 安装到手机
3. 跳到"二、使用应用"

### 选项 B：自己编译
1. 安装 Android Studio
2. 打开项目 `DvplModHelper`
3. 点击运行按钮 ▶️

## 二、使用应用（3 分钟）

### 场景 1：解密游戏资源（DVPL → 原文件）

**目标**：把压缩的 `.dvpl` 文件解码，以便查看和修改

1. 打开应用
2. 点击 **"解码 DVPL → 原文件（支持多选）"**
3. 选择 .dvpl 文件
   ⚠️ Android 11+ 的文件选择器无法直接进入游戏的 Android/data 目录，
   请先用系统文件管理器或电脑把 .dvpl 文件复制到 Download 等普通目录
4. 等待几秒，完成！
5. 解码后的文件保存在导出目录（默认 Download/DVPLModHelper）

**示例**：
```
weapon.pck.dvpl  →  weapon.pck  ✅ 可以用音频编辑器打开
tank.tex.dvpl    →  tank.tex    ✅ 可以转换成图片
```

### 场景 2：编辑材质贴图（PVR → PNG → PVR）

**目标**：修改坦克皮肤、UI 图标等

#### 第一步：PVR 转 PNG（方便编辑）
1. 先用 DVPL 解码得到 `.pvr` 文件
2. 点击 **"PVR → PNG（支持批量）"**
3. 选择 `.pvr` 文件
4. 得到 `.png` 图片 ✅ 用 Photoshop/GIMP 打开编辑

#### 第二步：编辑图片
- 用任意图片编辑器修改 PNG
- 注意保持尺寸和透明通道

#### 第三步：PNG 转回 PVR
1. 点击 **"PNG → PVR（选择压缩质量）"**
2. 选择编辑好的 PNG
3. 得到新的 `.pvr` 文件

#### 第四步：重新打包（PVR → DVPL）
1. 点击 **"编码为 DVPL（支持多选）"**
2. 选择新的 `.pvr` 文件
3. 压缩类型选 **"LZ4_HC"**（高压缩比）
4. 得到 `.dvpl` 文件
5. 替换游戏目录中的原文件

### 场景 3：批量处理（快速制作 Mod）

**目标**：一次处理几十个文件

1. 点击对应功能
2. **多选文件**（长按 → 全选）
3. 等待进度条完成
4. 所有文件自动处理

**技巧**：
- 文件越多，多线程优势越明显
- 10 个文件几乎和 1 个文件一样快

## 三、常见工作流

### Mod 制作完整流程

```
1. 从游戏提取资源
   ├─ 找到游戏数据目录
   └─ 备份原始文件 ⚠️

2. 解码 DVPL
   └─ DVPL 解码 → 得到原始文件

3. 转换格式（如果是贴图）
   └─ PVR 转 PNG → 得到可编辑图片

4. 编辑资源
   ├─ 图片：Photoshop/GIMP
   ├─ 音频：Audacity
   └─ 模型：Blender

5. 转回游戏格式
   └─ PNG 转 PVR

6. 重新打包
   └─ DVPL 编码 → 得到 .dvpl

7. 替换文件
   ├─ 复制到游戏目录
   └─ 重启游戏测试
```

### 快速测试 Mod

```bash
# 1. 准备测试数据
echo "Test Data" > test.txt

# 2. 编码
点击"DVPL 编码" → 选择 test.txt → 得到 test.txt.dvpl

# 3. 解码
点击"DVPL 解码" → 选择 test.txt.dvpl → 得到 test.txt（恢复）

# 4. 验证
对比两个 test.txt 是否一致 ✅
```

## 四、高级技巧

### 压缩类型选择

| 场景 | 推荐类型 | 理由 |
|-----|---------|------|
| 小文件 (< 1KB) | 自动/不压缩 | 压缩反而更大 |
| 文本/代码 | LZ4_HC | 压缩比高 |
| 已压缩格式 (PNG/MP3) | 不压缩 | 再压缩无意义 |
| 大量小文件 | LZ4 | 速度快 |
| 最终发布 | LZ4_HC | 节省空间 |

### 性能优化

- **批量处理**：一次选择多个文件，利用多线程
- **大文件**：自动流式处理，不会爆内存
- **减少转换**：直接编辑 PNG，最后一次性转换

### 故障排除

| 问题 | 解决方案 |
|-----|---------|
| 解码失败 | 检查文件是否损坏，尝试不同压缩类型 |
| PVR 转换失败 | 确保是 PowerVR 格式（游戏用） |
| 应用崩溃 | 检查存储权限，清理应用数据 |
| 文件找不到 | 使用文件管理器确认路径 |

## 五、示例项目

### 示例 1：更换坦克图标
```
files/UI/Icons/Tank_IS7.pvr.dvpl
  ↓ DVPL 解码
Tank_IS7.pvr
  ↓ PVR 转 PNG
Tank_IS7.png  ← 用 Photoshop 编辑
  ↓ PNG 转 PVR
Tank_IS7.pvr
  ↓ DVPL 编码 (LZ4_HC)
Tank_IS7.pvr.dvpl  ← 替换游戏文件
```

### 示例 2：修改音效
```
WwiseSound/weapon.pck.dvpl
  ↓ DVPL 解码
weapon.pck  ← 用 Wwise 工具打开
  （编辑音频）
  ↓ DVPL 编码 (LZ4)
weapon.pck.dvpl  ← 替换游戏文件
```

## 需要帮助？

- 📖 详细文档：[README.md](README.md)
- 🐛 问题反馈：GitHub Issues
- 🐛 问题反馈：GitHub Issues

---

**祝你 Mod 制作愉快！🎉**